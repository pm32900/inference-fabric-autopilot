apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "autopilot.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "autopilot.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{ .Values.config | toYaml | nindent 4 }}
