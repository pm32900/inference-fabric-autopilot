apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: inference-autopilot
spec:
  selector:
    app: control-plane
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP